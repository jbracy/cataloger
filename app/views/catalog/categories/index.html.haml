.container_12
  .grid_3
    .categories
      %h3 Edit Categories<span class="arrow"></span>

      %ul
        - @catalog.categories.each do |category|
          %li
            =category.name
            =link_to "Edit", edit_catalog_category_path(@catalog,category)
            =link_to "Delete", catalog_category_path(@catalog, category), :method => :delete, :confirm => "Are you sure?"

      =link_to "Add", new_catalog_category_path